# Waveform documentation site

Run `npm ci && npm run build && npm run check`. Canonical Markdown lives in `../docs`. CLI releases use the root Honeycomb manifest and the six-target release workflow. Deploy `dist` to the dedicated Vercel project `silicon-waveform-docs`, with `docs.waveform.teamofsilicons.com` attached.
