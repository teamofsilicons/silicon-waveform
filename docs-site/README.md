# Waveform documentation site

Run `npm ci && npm run build && npm run check`. Canonical Markdown lives in `../docs`. Package the CLI and client source snapshot as `waveform-source.tar.gz` before building. Deploy `dist` to the dedicated Vercel project `silicon-waveform-docs`, with `docs.waveform.teamofsilicons.com` attached.
